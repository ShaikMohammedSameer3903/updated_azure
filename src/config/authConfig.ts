// ============================================================
// Auth Config — Re-exports from msalConfig (single source of truth)
// ============================================================

export {
  msalConfig,
  loginRequest,
  azureTokenRequest,
  graphTokenRequest,
  graphScopes,
  azureManagementScopes,
  appRoles,
  isDemoMode,
  popupRedirectUri,
} from './msalConfig';
